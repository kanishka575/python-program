# nesting (loop inside the loop is basically the nesting )    
age = 90

if(age >= 18):
    if(age >= 80):
      print("cannot drive")
    else:
      print("can drive")
else:
    print("cannot drive")  